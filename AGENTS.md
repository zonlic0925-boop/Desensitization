# 图纸脱敏项目 Agent 宪法

## 项目定位

本地桌面工具：读入客户 PDF 工程图纸，抹除 Fisher Controls / Emerson / TopWorx / MKS 系列公司名、Logo、保密标记（CONFIDENTIAL 类）等敏感信息，抹除严格限制在敏感信息所处方框内、不越框污染图纸其他信息，输出按"原名 + `_desensitized` 后缀"生成脱敏后 PDF（如 `AA01_1K4168_A.pdf` → `AA01_1K4168_A_desensitized.pdf`）。全程本地离线，不修改客户原始文件。

本文件是项目唯一宪法真源（opencode 自动读取根目录 AGENTS.md）。

- `skills-master/` 是外部宪法模板参考仓库，其中任何规则不结算为项目规则，不参与构建。
- 本期范围：仅可运行源码 + 样本回归；打包/安装器（PyInstaller）为后置阶段，不在本期验收命令内。

## 核心原则

- 架构优先、设计优先、真源优先、严格验收；开始编码前必须先想清楚为什么这么做、是否正确、是否有更简单做法。
- agent 必须敢于指出不合理需求、错误架构、冲突规则和缺失验收，帮助完善系统，不机械执行。
- 禁止补丁式开发：不得用散落 if、临时兼容分支、硬编码敏感词、sleep、mock 绕过架构问题。
- 禁止选项剧场：方案给一个推荐主线，备选只用于解释为什么不选。
- 用户明确否定的概念必须删除且不得换名复活：DXF/DWG 输入通道、联网/云脱敏、深度学习 inpainting 抹除、全图盲搜敏感文字。
- 当用户说"先看/不动代码/先给判断"时，保持只读审查，直到明确要求执行。

## 思考偏好（用户工作区规则，与模板纪律并列生效）

### 核心要求

1. **复述理解**：每次接到任务，先用一句话复述对任务要求的理解，确认对齐后再行动。
2. **第一性原理洞察**：不依赖类比或惯例，从问题最基础的物理/逻辑事实出发，拆解任务本质。
3. **深挖目标障碍**：先找出阻碍目标达成的核心障碍，把障碍转化为真正要解决的问题——很多时候"如何做X"的真正问题是"如何消除阻止X的条件"。
4. **识别约束和隐性假设**：复杂问题先剥离出真正的约束条件（物理限制、资源限制、时间限制）和隐性假设（那些被当作"理所当然"但未必成立的前提）。
5. **因果链分析**：原因分析至少深入 3-5 层，列出完整因果链，不停留在表面症状。
6. **输出风格**：专业但不过于学术，说人话，避免官话套话和正确的废话。

### 思考偏好

- **第一性原理是默认思考模式**：遇到任何设计、决策、调试问题，默认用第一性原理拆解，除非问题确实简单到不需要。
- **TOC 约束理论做决策**：面临资源分配、优先级排序、优化方向选择时，调用 TOC 五步法：
  1. **识别**系统的当前瓶颈约束
  2. **挖掘**约束（充分利用瓶颈，不做浪费瓶颈产能的事）
  3. **服从**约束（让其他环节配合瓶颈节奏）
  4. **打破**约束（如果挖掘+服从后仍不够，提升瓶颈产能）
  5. **回到**第一步（打破旧约束后，新约束必然出现，循环继续）
- **决策清晰化**：不模糊决策，明确指出"基于什么约束，选择了什么方案，放弃了什么"。

### 执行检查清单

每次任务开始前快速过一遍：
- [ ] 我是否复述并确认了任务理解？
- [ ] 问题的本质是什么，而非表面需求？
- [ ] 核心障碍是什么？它可被消除吗，还是必须绕过？
- [ ] 有哪些隐性假设？它们成立吗？
- [ ] 当前系统的瓶颈约束在哪里？
- [ ] 因果链是否足够深（至少 3 层）？

## 真源优先级

事实冲突时按以下顺序处理：

1. 当前源码、测试、样本图纸、运行日志、实机/用户侧证据、当前 git 状态。
2. 本宪法文件（项目唯一宪法真源）。
3. `docs/README.md`（内部真源索引，待建）。
4. 当前 active 的产品评审、架构、合同、实现计划、验收、审计文档。
5. 当前代码或用户仍确认有效的 README、issue、review comments、commit history。
6. 历史归档、旧会话、旧记忆只能作为模式证据，不能覆盖当前项目事实。

如果真源缺失、互相矛盾或技术上明显错误，先报告冲突、证据和推荐处理方式，再等用户确认。

## 必需工作流

对立项、接管、功能、重构、修 bug、发布和文档任务，默认遵循：

```text
当前真源审计
  -> 推理闸
  -> 唯一 owner 与合同设计
  -> 测试 / 样本 / 门禁计划
  -> 核心实现（core/）
  -> 薄接线（ui/ 只做展示与交互，不持有业务语义）
  -> 针对性验收
  -> 文档回写
  -> git 边界复核
```

空项目启动时，禁止直接写代码或选择框架；必须先完成产品定义、唯一主线、第一闭环、技术栈确认和验收标准（本项目已完成，见上方项目定位与验收规则）。

半路接管本项目时，禁止把已有项目当空项目重写；必须先做只读接管审计，确认已有代码、入口、调用链、接口契约、样本图纸、测试脚本、配置、未提交变更和文档线索。

## 推理闸

编码前至少回答以下问题：

- 实际要解决的问题是什么？
- 谁创建这个概念，谁调用它，谁消费它？
- 当前真源在哪里？是否已经有同职责模块、合同、schema、服务或文档？
- 唯一 owner 是哪一层或哪一个模块？哪些层禁止成为 owner？
- 是否跨 UI/API/CLI 等多个入口？如果跨入口，语义必须先进入共享合同或 core 层。
- 更简单、更保守的设计是什么？为什么不够？
- 最大回归风险是什么？用什么测试、日志、渲染截图或样本证据阻断？

推理闸没有闭合，不开始实现。

## 错误分级

审查、实现和验收时必须把问题分级：

- 阻断问题：破坏核心脱敏链路、抹除越框污染、安全/隐私、样本回归失败、关键命令不可用；当轮必须收掉。
- 设计风险：不一定立即破坏，但会导致架构漂移或后续难维护；必须说明取舍和验收入口。
- 可记录债务：不影响本轮目标链路，可暂缓；必须说明不处理原因和后续入口。
- 无关优化：不进入本轮范围，禁止借机扩大改造。

禁止用情绪强度替代工程优先级。没有证据的担忧不能写成阻断；有证据的阻断也不能降级成"后面再说"。

## 项目技术规则

- 技术栈（以 `pip list` 实测为准）：Python 3.11.9 / PyQt5 5.15.11 / PyMuPDF 1.27.1 / rapidocr-onnxruntime 1.4.4 / opencv-python / numpy / pytest。语言、框架习惯遵循各自生态最佳实践，禁止私造并行体系。
- 输入只接受 PDF。按内容形态分三路：文字层 PDF 走矢量通道（PyMuPDF 提取 span 坐标）、纯栅格 PDF 走 OCR 通道（渲染 300dpi → RapidOCR 检测框）、混合 PDF 双通道并行后统一坐标系、IoU 去重融合。
- Logo 等图形敏感对象不依赖 OCR：用 PyMuPDF 提取页内图片对象（XObject image）坐标，标题栏/边框区内的图片对象自动高置信命中。
- 敏感词匹配走 `rules/` 外部词表驱动：Fisher Controls、Emerson、TopWorx、MKS 系列及 CONFIDENTIAL 类标记；禁止在代码中硬编码敏感词，词表变更走文件不改代码。
- 抹除执行默认 `page.apply_redactions()` 真删除；黑白块覆盖为可切换模式（UI 选项）。两种模式都必须过验收三重证据。
- 抹除框来自框线归位（box_finder 自研模块）：敏感文字框向上归位到最小封闭表格单元格；无框可归位者退化为"文字框 + padding"并强制标"待人工确认"，不得自动执行。
- 搜索区域先验：标题栏/边框区/页眉页脚为默认搜索区；视图区正常标注（尺寸、公差、技术要求）为受保护内容，规则引擎须有白名单防误抹。

## 实现规则

- 从合同、数据模型、核心文档开始，而不是从 UI、脚本倒推核心。共享语义必须单一真源，进入 `core/` 合同层，不散落在 UI、脚本或提示词里。
- 添加测试、样本或 gate 脚本时，应放在风险真正所属的层级。
- 使用结构化 API、解析器和本地 helper，而不是脆弱的字符串拼接和临时路径判断。
- 不用旧记忆、旧输出或"应该是这样"代替当前命令和当前代码证据。
- 修改代码、接口、配置、架构边界、用户行为或产品语义后，必须同步项目文档。
- 遇到 review 反馈或用户质疑时，先查当前实现、框架底层和可复现证据，再决定是否接受修改。
- 生产路径必须用项目统一错误类型、结构化错误返回失败；禁止裸异常、固定字符串错误或静默吞错处理可恢复问题。
- 配置、词表、路径必须显式下传或通过项目既有配置系统读取；禁止全局可变静态、硬编码路径。
- 观测与诊断（命中项、置信度、耗时、失败原因）必须来自真实数据源；拿不到数据就返回 N/A 或结构化不可用原因，禁止固定值、占位值和伪健康状态。
- 用户可见文案与代码注释、内部文档分层，不混写；界面正文使用中文。

## 验收规则

- 验收必须匹配改动风险，不能只跑最轻命令就宣布完成。
- 声称"完成""修好""通过"前，必须提供本轮实际运行过的命令、测试、日志、渲染证据或用户侧证据。
- **脱敏验收三重证据（本项目特有，缺一不可）**：
  1. 输出 PDF 文本层检索：PyMuPDF 全文搜索全部敏感词 → 零命中；
  2. 渲染目检：输出 PDF 按页渲染成图，逐一核对抹除块未越框线、不污染框外标注；
  3. 样本回归：`Testing Drawings/` 全量样本跑完三通道，命中项清单与人工核对记录归档。
- 发现失败时，先复现、收集证据、建立可证伪假设，再修改。禁止无复现地猜测原因。
- UI 改动必须检查真实渲染、交互、加载态、错误态；不得只看代码。
- OCR 相关改动必须覆盖纯栅格 PDF 与 `*_Markup_*` 批注版样本。
- warning、lint warning、类型告警、格式漂移、文档索引缺失和本轮 TODO/TBD 都按缺陷处理；除非明确记录为非本轮债务，否则不能带着它们声称完成。
- 测试若依赖隔离状态，必须显式构造隔离边界；单样本快照测试不能伪装成全局累计事实。

## 资源与性能

- 单页渲染分辨率、OCR 并发数、批处理内存上界必须有明确预算；大样本批处理不得无界常驻。
- 独立 I/O 应并行；重复计算应缓存或下沉到合适层；性能优化先定位真实瓶颈，不牺牲正确性。
- OCR 缺模型/缺依赖/失败时返回结构化不可用原因，禁止固定占位结果。

## 多 Agent 规则

- 任务能拆成互不干扰的独立域时优先使用多 agent：矢量通道、OCR 通道、框线归位、UI 可并行，文件与模块 owner 不得重叠，集成时复核冲突。
- 每个子 agent 必须有明确范围、输入、禁止事项和期望输出。
- 不把当前关键路径上的阻塞任务交给子 agent 后原地等待；主 agent 必须继续做不重叠的工作。
- 子 agent 的结论不是最终真源；最终结论仍要由主 agent 对照当前代码、测试和样本验收。

## 文档分层

- 内部真源：架构、宪法、owner map、验收门禁、治理历史、审计结论、阶段计划 → `docs/`（索引 `docs/README.md`）。
- 用户文档：操作手册、敏感词表维护说明、输出命名规则 → 单独用户文档，只写使用、配置、排障，不混写内部机制。
- 同一变更同时影响用户行为和内部机制时，必须分别写入对应层。
- 新增当前真源文档必须被真源索引引用；完成、失效、被吸收或一次性的文档必须归档或标注非当前，不能留在主目录误导后续 agent。

## Git 规则

- 当前项目不是 git 仓库。若用户初始化 git，遵守：提交前确认 git root、`git status --short`、ignored 文件、staged 列表和嵌套仓库边界；禁止 `git add .`，只 stage 与本任务相关文件；dirty worktree 中不得覆盖用户未授权改动；不使用破坏性 git 命令，除非用户明确要求并确认风险。
- 客户原始图纸属受保护数据：任何情况下不得提交、上传、写入日志或出现在文档示例中（测试样本本身除外）。

## 必须停止并询问

- 用户需求、当前代码、本宪法、真源文档或验收结果互相冲突。
- 需要无意义兼容、保留旧路线残影或同时支持互斥路线。
- 需要创建、改写或废弃本宪法、真源索引、核心 API、词表合同或输出命名规则。
- 需要删除重要功能、接口、配置或已确认的验收门禁。
- 当前证据表明用户的目标会损害脱敏正确性、数据安全或用户体验。
- 上下文不足以判断 owner 边界，并且猜测会造成不可逆或大范围影响。

停止时不要只说"无法继续"。必须给出：冲突证据、可选处理方向、推荐方向、需要用户确认的问题。

## 交接规则

当上下文过大、需要换窗口、需要交给另一个 agent，或任务尚未闭合时，必须产出可直接复制的交接文本，包含：

- 当前目标和已确认的产品/架构边界。
- 当前 git 状态、已改文件、未提交文件和嵌套仓库状态。
- 已完成工作、实际验收命令和结果。
- 未闭合风险、漂移警告和禁止触碰的用户改动。
- 下一步最安全命令和停止条件。